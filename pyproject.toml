[project]
name = "dd-hp-pdim"
version = "0.1.0"
description = "Data-Driven Neural Network Hyperparameter Tuning"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "numpy>=2.0.2",
    "scipy>=1.13.1",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
]
